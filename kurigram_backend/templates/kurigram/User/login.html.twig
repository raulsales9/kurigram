{% extends "layout.html.twig" %}
{% block content %}
    <section class="container">
        <div class="row mt-5 mb-5" style="border: 1px solid black">
            <div class="col-xl-6 col-lg-6 col-md-12 mt-5 mb-5">
				
				<form>
                {% if error %}
                    <div class="alert">
                        {{ error.messageKey|trans(error.messageData, 'security') }}
                    </div>
                {% endif %}
                {% if app.user %}
                    <div class="mb-3 mt-3">
                        Estás dado de alta como: {{ app.user.userIdentifier }} <a href="{{ path('app_logout') }}" class="btn btn-primary">Cerrar Sesión</a>
                    </div>
                {% endif %}
                <h2 class="mt-3 mb-3">Iniciar sesion</h2>
					<div class="form-group mt-3 mb-3">
						<label for="nombre">Nombre</label>
						<input type="text" class="form-control form-control-md" value="{{ last_username }}" name="id_user" id="nombre" placeholder="Ingresa tu nombre">
					</div>
					<div class="form-group mt-3 mb-3">
						<label for="password">Contraseña</label>
						<input type="password" class="form-control form-control-sm" id="password" placeholder="Ingresa tu contraseña" autocomplete="current-password" required>
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}" >
                    </div>
					<button type="submit" class="btn btn-primary mt-3 mb-3">Iniciar Sesion</button>
				</form>
			</div>
            <div class="col-xl-6 col-lg-6 d-none d-lg-block" style="background-color: lightblue; height: 500px; "><img src="../../../images/logos/logo_kurigram_color.png" style="margin-top: 100px; margin-left: 150px"></div>
        </div>
    </section>
{% endblock %}
